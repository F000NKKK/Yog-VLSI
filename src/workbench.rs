//! Crafting workbench: block + GUI for placing/editing/fabricating microchips.

use yog_api::Registry;

pub fn register(_registry: &mut Registry) {
    // TODO: register workbench block + item
    // TODO: register UI (register_ui + on_ui_render) for chip editing
    // TODO: right-click interaction → open virtual redstone world (creative)
    // TODO: resource ammo system (paint-like refill, no storage limit)
}
