//! Microchip definitions: tiers, port list, circuit data, serialization.

use yog_api::Registry;

pub fn register(_registry: &mut Registry) {
    // TODO: register chip items per tier (wood, stone, gold, iron, diamond, netherite)
    // TODO: register chip NBT schema (ports, circuit data)
}
